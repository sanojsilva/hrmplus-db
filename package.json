{
	"name": "db",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"migrate:dev": "npx prisma migrate dev",
		"reset:dev": "npx prisma migrate reset",
		"db:push": "npx prisma db push",
		"gen": "npx prisma generate",
		"init": "npm install && node init.js && npm run db:push && npm run gen",
		"update": "git fetch && git pull && npm install && npm run db:push && npm run gen",
		"studio": "npx prisma studio"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"prisma": "^2.24.1"
	},
	"dependencies": {
		"@prisma/client": "^2.24.1"
	}
}
